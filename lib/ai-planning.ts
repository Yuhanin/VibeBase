export type RiskLevel='low'|'medium'|'high'
export type PlanFile={path:string;action:'create'|'update'|'delete';reason:string}
export type CodingPlan={version:'1.0';summary:string;steps:string[];files:PlanFile[];dependencies:string[];tests:string[];verification:string[];risks:{level:RiskLevel;description:string}[]}
const blocked=['.env','.env.local','.env.production','credentials','private_key','service_role']
export function validatePlan(plan:CodingPlan){if(plan.version!=='1.0')throw new Error('Unsupported plan version');if(!plan.summary||!plan.steps?.length)throw new Error('Plan must contain a summary and steps');for(const f of plan.files||[]){if(!f.path||f.path.startsWith('/')||f.path.includes('..')||blocked.some(x=>f.path.toLowerCase().includes(x)))throw new Error(`Unsafe plan path: ${f.path}`)}return plan}
export function planFromContext(ctx:any):CodingPlan{const title=ctx?.task?.title||'Task';return {version:'1.0',summary:`Implement ${title} using the project context and existing architecture.`,steps:['Review requirements and relevant project context','Identify minimal implementation surface','Implement changes with existing conventions','Run automated tests and validation','Verify the resulting workflow'],files:[],dependencies:[],tests:['typecheck','lint','relevant unit/integration tests'],verification:['CI passes','no unauthorized files changed','production smoke test passes'],risks:[{level:'medium',description:'Generated plan requires review before file execution.'}]}}
