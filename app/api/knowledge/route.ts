import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'
const db=()=>createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{autoRefreshToken:false,persistSession:false}})
export async function GET(request:Request){const projectId=new URL(request.url).searchParams.get('projectId');if(!projectId)return NextResponse.json({error:'projectId is required'},{status:400});const [docs,decisions,issues,releases]=await Promise.all([db().from('documents').select('*').eq('project_id',projectId).order('updated_at',{ascending:false}),db().from('decisions').select('*').eq('project_id',projectId).order('created_at',{ascending:false}),db().from('issues').select('*').eq('project_id',projectId).order('created_at',{ascending:false}),db().from('releases').select('*').eq('project_id',projectId).order('created_at',{ascending:false})]);return NextResponse.json({documents:docs.data||[],decisions:decisions.data||[],issues:issues.data||[],releases:releases.data||[]})}
export async function POST(request:Request){const b=await request.json();if(!b.projectId||!b.title?.trim()||!b.content?.trim())return NextResponse.json({error:'projectId, title and content are required'},{status:400});const {data,error}=await db().from('documents').insert({project_id:b.projectId,title:b.title.trim(),content:b.content.trim(),type:b.type??'documentation'}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({document:data},{status:201})}
export async function PATCH(request:Request){const b=await request.json();if(!b.id)return NextResponse.json({error:'id is required'},{status:400});const p:Record<string,unknown>={};for(const k of ['title','content','type'])if(b[k]!==undefined)p[k]=b[k];const {data,error}=await db().from('documents').update(p).eq('id',b.id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({document:data})}
export async function DELETE(request:Request){const id=new URL(request.url).searchParams.get('id');if(!id)return NextResponse.json({error:'id is required'},{status:400});const {error}=await db().from('documents').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
