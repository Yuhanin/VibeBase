import {NextResponse} from 'next/server';import {createSupabaseServer} from '../../../../lib/auth';
export async function GET(request:Request){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const taskId=new URL(request.url).searchParams.get('taskId');if(!taskId)return NextResponse.json({error:'taskId is required'},{status:400});const {data:t}=await s.from('tasks').select('id,github_branch').eq('id',taskId).single();if(!t)return NextResponse.json({error:'Task not found'},{status:404});return NextResponse.json({taskId:t.id,branch:t.github_branch||null,status:'pending',message:'CI status is read from GitHub after the task branch or PR exists.'})}
