import {NextResponse} from 'next/server';import {createSupabaseServer} from '../../../../lib/auth';import {validateChanges} from '../../../../lib/coding-agent';
export async function POST(req:Request){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await req.json();validateChanges(b.changes);return NextResponse.json({valid:true,fileCount:b.changes.length})}catch(e){return NextResponse.json({valid:false,error:e instanceof Error?e.message:'Invalid changes'},{status:400})}}
