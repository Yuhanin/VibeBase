import {NextResponse} from 'next/server';import {createSupabaseServer} from '../../../../lib/auth';
export async function GET(){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json({version:'1.0',instructions:['Inspect repository before changing files','Use only structured file changes','Never read, print, or modify secrets','Never execute arbitrary shell commands','Keep changes scoped to the task','Add or update tests when behavior changes','Return a concise summary, changed files, and verification results']})}
