import {NextResponse} from 'next/server';import {createSupabaseServer} from '../../../../lib/auth';
export async function GET(req:Request){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const id=new URL(req.url).searchParams.get('taskId');if(!id)return NextResponse.json({error:'taskId is required'},{status:400});const {data:rows,error}=await s.from('audit_logs').select('*').eq('task_id',id).order('created_at',{ascending:false}).limit(50);if(error)return NextResponse.json({events:[]});return NextResponse.json({events:rows||[]})}
