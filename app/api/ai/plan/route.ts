import {NextResponse} from 'next/server'
import {createSupabaseServer} from '../../../../lib/auth'
export async function POST(req:Request){const s=await createSupabaseServer();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.request?.trim())return NextResponse.json({error:'request is required'},{status:400});const {data:projects}=await s.from('projects').select('name,description,problem,target_audience,vision,tech_stack').limit(20);return NextResponse.json({plan:{goal:b.request.trim(),context:projects||[],steps:['Analyze requirements','Inspect affected architecture','Implement smallest safe change','Run tests and type checks','Review and prepare release'],acceptanceCriteria:['Requirement implemented','No secrets committed','Existing behavior preserved','Tests pass']}})}
