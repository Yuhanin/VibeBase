'use client'
import {useEffect,useState} from 'react'
export default function GitHubActivity(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/github/activity').then(r=>r.json()).then(setD)},[]);return <main className="min-h-screen p-6 md:p-10"><div className="mx-auto max-w-6xl"><p className="text-sm tracking-[.2em] text-zinc-500">VIBEBASE / GITHUB ACTIVITY</p><h1 className="mt-2 text-3xl font-bold">Coding activity</h1><div className="mt-8 grid gap-4 md:grid-cols-3"><Stat title="Open issues" value={d?.openIssues??'—'}/><Stat title="Open pull requests" value={d?.openPullRequests??'—'}/><Stat title="Recent commits" value={d?.commits?.length??'—'}/></div><section className="mt-6 rounded-2xl border border-zinc-800 bg-zinc-950 p-6"><h2 className="font-semibold">Recent commits</h2><div className="mt-4 space-y-3">{(d?.commits||[]).map((c:any)=><div key={c.sha} className="rounded-xl border border-zinc-800 p-4"><p className="text-sm">{c.message}</p><p className="mt-1 font-mono text-xs text-zinc-600">{c.sha.slice(0,8)}</p></div>)}</div></section></div></main>}
function Stat({title,value}:{title:string,value:any}){return <div className="rounded-2xl border border-zinc-800 p-5"><p className="text-xs text-zinc-500">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>}
